# 공부하자역사앱 — Design.md (2차: 흥미 유발 디자인)

> 작성일: 2026-08-18
> 상태: 확정 완료 — 구현 단계 진행 가능
> 선행 문서: Design.md (1차 — 학생별 파스텔 테마). 본 문서는 1차 완료를 전제로 한 후속 작업이다.

---

## 0. Scope Lock (범위 고정)

**이번 작업은 "중·고등학생이 공부에 흥미를 갖도록" 하는 비주얼/피드백 강화이며, 기존 기능은 100% 유지한다.** 게이미피케이션 시스템(포인트/레벨/보상 등 새 기능)은 이번 범위에서 제외한다.

### 변경 대상
- 정답 시 시각 피드백 (반짝임/통통 튀는 애니메이션)
- 진행바가 차오르는 애니메이션
- 성취 배지 (심플한 아이콘 + 텍스트 스타일)
- 기존 효과음 기능의 UI/UX 다듬기 (기능 자체는 이미 존재 — 아래 참고)
- 1차의 파스텔 캔디톤을 더 생기발랄하게 강화

### 변경하지 않는 것 (절대 손대지 않음)
- 효과음 **재생 로직** (`SFX` 객체, `AudioContext` 합성, `toggleAppSound()` 등) — 이미 완성되어 있으므로 로직은 건드리지 않고, 필요 시 호출 지점/토글 버튼의 위치·스타일만 조정
- `SOUND_MUTE_KEY`(`appSoundMuted`) localStorage 키 및 음소거 저장 방식 — 그대로 사용
- GAS 백엔드 로직, 진행률 계산, 학생 ID 매핑, LEARNING_MODULES
- 폰트 선택 기능, 아바타 기능
- 사건배열게임 서브 테마
- 새로운 동기부여 기능(포인트/레벨/랭킹/출석보상 등) — 범위 밖, 필요 시 별도 기능 문서(PRD)로 분리
- HTML id/class 이름 (스타일링용 class 추가는 가능, 기존 참조 요소 변경 시 사전 확인)

### 대상 사용자
중·고등학생 (중1~고3, 13~18세 혼합). 유치하지 않으면서도 딱딱하지 않은, "생기있지만 과하지 않은" 톤을 유지한다.

---

## 1. 기존 자산 조사 (재확인)

작업 전 코드 조사 결과, 아래 기능이 **이미 구현되어 있어 새로 만들 필요가 없다.** 디자인 작업은 이 위에 얹는 방식으로 진행한다.

### 1.1 효과음 (이미 완성됨 ✅)
- `SFX` 객체가 Web Audio API로 효과음을 즉석 합성 (외부 mp3 파일 불필요)
  - `SFX.correct()` — 정답음 (2음 상승)
  - `SFX.wrong()` — 오답음
  - `SFX.complete()` — 완료음 (4음 팡파레)
  - `SFX.click()` — 버튼 클릭음 (이벤트 위임으로 전체 버튼에 자동 적용)
- **음소거 토글**: `toggleAppSound()` + `isAppSoundMuted()`, 상태는 `localStorage`(`appSoundMuted`)에 저장 → 재접속 시 유지됨
- 토글 버튼: `#sound-toggle-btn` (🔊/🔇 아이콘 전환, `updateSoundToggleBtn_()`)
- 배경음악(BGM) 기능도 존재 (`BGM_PLAYLIST`)

> **결론**: "정답/완료 효과음 + 켜고 끄는 옵션 + 저장"은 요구사항이 이미 충족되어 있음. 이번 작업은 (a) 효과음이 **시각 효과와 동시에** 터지도록 호출 지점을 맞추고, (b) 토글 버튼을 새 디자인 톤에 맞게 스타일링하는 정도.

### 1.2 접근성
- 기존 `prefers-reduced-motion` 대응 존재 → 새 애니메이션도 반드시 이 미디어쿼리 안에서 비활성화/약화 처리

---

## 2. To-be (흥미 유발 디자인 방향)

### 2.1 정답 시 시각 피드백
정답을 맞혔을 때 순간적으로 "잘했다"는 느낌을 주는 가벼운 모션.

- **반짝임(sparkle)**: 정답 선택지 주변에 짧은 반짝임 효과 (0.4~0.6초)
- **통통 튀기(bounce/pop)**: 정답 카드/버튼이 살짝 커졌다 돌아오는 스케일 애니메이션
- 색상은 1차에서 정한 `--state-correct`(#4FAE6B) 계열 사용
- 기존 `SFX.correct()` 효과음과 **동시에** 재생 (호출 타이밍만 맞춤)
- 과하지 않게 — 중·고생 대상이므로 유아용처럼 요란하지 않고 "깔끔하게 기분 좋은" 수준

### 2.2 진행바 애니메이션
진행률이 올라갈 때 막대가 부드럽게 차오르는 모션.

- 진행바 채워지는 부분에 `transition: width` 적용 (0.6~0.8초, ease-out)
- 값이 올라갈 때만 애니메이션 (내려갈 땐 즉시)
- 학생색(`--student-accent`)으로 채워짐 (1차 테마 연동)
- 선택적: 목표치 도달 시(예: 100%) 가벼운 반짝임 1회

### 2.3 성취 배지 (심플 아이콘 + 텍스트)
단원/PART 완료, 마일스톤 달성 시 표시되는 배지. **새로운 보상 시스템이 아니라, 이미 계산되는 완료 상태를 시각적으로 보여주는 표식.**

- 스타일: 심플한 아이콘(Tabler Icons 등 기존 아이콘셋) + 짧은 텍스트 라벨
- 예: `✓ 역사총정리① 완료`, `📐 수학 3단원 클리어` (아이콘은 라인 스타일 통일)
- 배경: 학생색 tint 또는 뉴트럴, 알약형(pill) radius
- 유치한 캐릭터/이모지 남발 대신 **깔끔한 아이콘 + 텍스트**로 중·고생 눈높이에 맞춤
- 배지는 기존에 완료 데이터가 있는 위치(학습홈, 스터디플래너의 "완료한 단원" 등)에 얹음

### 2.4 효과음 UX 다듬기
- 기존 `#sound-toggle-btn`을 새 디자인 톤에 맞게 재스타일링 (아이콘, 위치)
- 정답/완료 시각 효과가 나올 때 대응하는 `SFX.correct()`/`SFX.complete()`가 함께 재생되는지 확인 (이미 연결돼 있으면 유지)
- 사용자가 음소거 상태여도 **시각 효과는 항상 표시** (소리만 꺼짐)

### 2.5 파스텔톤 생기 강화
1차의 파스텔 캔디톤을 유지하되, 살짝 더 활기있게.

- 채도를 아주 약간 올리거나, 포인트 요소에 미세한 그라데이션/광택 검토
- 단, 1차에서 확정한 학생색/과목색/뉴트럴 팔레트의 **기본 hex는 유지** — 위에 얹는 강조 효과로만 생기 부여 (팔레트를 다시 갈아엎지 않음)

### 2.6 접근성 (필수)
- 위 모든 애니메이션은 `@media (prefers-reduced-motion: reduce)` 안에서 비활성화 또는 최소화
- 움직임 최소화를 선호하는 사용자는 애니메이션 없이 결과 상태(정답 표시, 채워진 진행바, 배지)만 즉시 보게 됨
- 효과음은 이미 사용자가 끌 수 있음 (기존 기능)

---

## 3. 파일별 예상 변경 범위

| 파일 | 변경 내용 | 비고 |
|---|---|---|
| `index.html` | `<style>`에 애니메이션 `@keyframes`(sparkle, pop, progress-fill 등) 추가, 배지/토글버튼 스타일, `prefers-reduced-motion` 블록에 신규 애니메이션 무력화 규칙 추가 | 기존 keyframes와 이름 충돌 없도록 신규 이름 사용 |
| `app.js` | 정답/완료 처리 지점에서 시각 효과 class를 잠깐 부여하는 최소 코드 추가 (효과음은 이미 호출 중이면 그대로) | 기존 로직 변경 없이 "class 추가/제거"만. `SFX` 로직은 손대지 않음 |

---

## 4. 확정 사항 요약

| 항목 | 결정 |
|---|---|
| 흥미 유발 수준 | 비주얼 + 약간의 재미요소 (게이미피케이션 제외) |
| 분위기 | 파스텔 캔디톤 유지 + 생기 강화 |
| 연령대 | 중·고 혼합 (유치하지 않게) |
| 재미요소 위치 | 정답 시 반짝임/통통, 진행바 차오름 |
| 배지 | 심플 아이콘 + 텍스트 |
| 효과음 | 기존 기능 활용 (정답/완료음 + on/off 저장), UI만 다듬기 |
| 접근성 | prefers-reduced-motion 대응 포함 |

---

## 5. 작업 순서 (제안)

1. `index.html`에 신규 애니메이션 `@keyframes` + 배지/토글 스타일 정의 (아직 적용 X)
2. `prefers-reduced-motion` 블록에 신규 애니메이션 무력화 규칙 추가 (접근성 먼저 확보)
3. 퀴즈 화면 정답 피드백에 시범 적용 → 확인 → 승인
4. 진행바 애니메이션 적용 → 확인
5. 배지 스타일 적용 (학습홈/스터디플래너 완료 표식) → 확인
6. 효과음 토글 버튼 재스타일링 + 정답/완료 효과음 동시 재생 확인
7. 전체 점검 (기존 기능·효과음·사건배열게임 무변경 확인)

각 단계는 별도 승인 후 진행하며, `app.js`는 class 부여/제거 수준의 최소 변경만 한다.
