# 공부하자역사앱 — Design.md

> 작성일: 2026-08-18
> 상태: 확정 완료 — 구현 단계 진행 가능

---

## 0. Scope Lock (범위 고정)

**이번 작업은 디자인(시각적 요소) 변경만 포함하며, 기존 기능은 100% 유지한다.**

### 변경 대상
- 색상 팔레트 (컬러 시스템 전면 재정의)
- 타이포그래피 톤 (폰트 선택 기능은 유지, 기본값/스타일 조정)
- 컴포넌트 스타일 (버튼, 카드, 진행바, 배지 등 시각 스타일)
- 디자인 토큰 정리 (radius/shadow 값, 여백 값 통일)
- 레이아웃 (요소 배치, 여백 구조, 정보 순서 등 시각적 배치 개선)
- 앱 로고 (2.7절 참고)

### 변경하지 않는 것 (절대 손대지 않음)
- GAS 백엔드 로직 전체 (파일명 미상 — 추후 확인 필요, 확인 전까지는 이번 작업에서 어떤 `.gs` 파일도 열람·수정하지 않음)
- `app.js`의 비즈니스 로직 (데이터 흐름, 이벤트 핸들러, 상태 관리, `loadHistoryTrainingProgress()` 등 최근 수정된 로직 포함)
- 학생 ID 매핑, 진행률 계산 로직, LEARNING_MODULES 구조
- 폰트 선택 기능(`FONT_OPTIONS`, `applyFont()`) — UI 스타일만 조정, 기능 자체는 유지
- `MathProgress` 관련 Stage 2-A 설계 및 통신 로직
- **"사건 배열하기" 게임의 고서/두루마리 컨셉 (`--ink`, `--parchment`, `--brass` 등 서브 테마 전체)** — 이번 리뉴얼 범위에서 제외. 기존 색상·폰트(`Song Myung`, `Noto Serif KR`) 그대로 유지, 손대지 않음 (2026-08-18 결정)
- HTML 구조(id, class 이름 중 JS에서 참조하는 것들) — 스타일링을 위해 class 추가는 가능하나 기존 id/class 제거·변경 시 app.js 참조 여부를 반드시 사전 확인 후 개별 승인

### 작업 원칙
- 파일 수정 전 반드시 명시적 승인
- 한 화면씩 단계적으로 진행 (일괄 변경 금지)
- 로컬 검증과 실제 배포 검증을 구분하여 보고

---

## 1. As-is (현재 디자인 조사 결과)

### 1.1 구조
- CSS는 `index.html` 내부 `<style>` 태그 하나(198~2573번째 줄, 약 2,376줄)에 전부 작성되어 있음. 별도 CSS 파일 없음.
- 화면(screen) 18개가 SPA 방식(`display:none` 토글)으로 관리됨: 시작/퀴즈/결과/요약/강의/지도학습/왕조배열게임/사건배열게임/교사·학부모 화면 등

### 1.2 색상 — 두 개의 테마가 공존, 하드코딩 다수
| 구분 | 변수 | 값 | 용도 |
|---|---|---|---|
| 메인 | `--stone` | #F4F7FC | 배경 |
| 메인 | `--rock` | #FFFFFF | 카드 배경 |
| 메인 | `--sand` | #7C8698 | 보조 텍스트 |
| 메인 | `--clay` | #A9B2C3 | 연한 회색 |
| 메인 | `--ember` | #3D7BF5 | 포인트(파랑) |
| 메인 | `--ash` | #1C2536 | 진한 텍스트 |
| 메인 | `--correct` / `--wrong` | #34C77B / #F0555C | 정답/오답 |
| 서브(사건배열게임) | `--ink`, `--parchment`, `--brass` 등 | #F5F4FB, #FFFFFF, #C9A227 | 고서/두루마리 컨셉 |

- 변수 밖에서 하드코딩된 색상이 다수: 파랑 계열 6종(#3D7BF5, #349BFE, #2F63D6, #2167ad, #3979c9, #6FA1FF), 초록 계열 6종, 빨강 계열 3종 등이 통일 없이 산재
- **문제**: 디자인 토큰이 없어 색상 변경 시 전체 검색·치환이 필요한 구조

### 1.3 타이포그래피
- 기본 폰트는 CSS 변수 `--app-font`로 제어되며, **사용자가 앱 내에서 직접 폰트를 선택하는 기능**이 존재 (`FONT_OPTIONS`): Gaegu(손글씨), Noto Sans KR(기본), Pretendard(모던), Gowun Dodum(둥근체), Nanum Myeongjo(붓글씨) → **기존 기능이므로 반드시 유지**
- 서브 테마(사건배열게임)는 `Song Myung`, `Noto Serif KR`을 별도로 사용해 톤이 이질적

### 1.4 컴포넌트
- `.btn-primary` / `.btn-secondary`는 사건배열게임 전용, 메인 화면은 `.start-btn`/`.next-btn`/`.home-btn`/`.retry-btn` 등 화면별 개별 클래스로 파편화 (다만 `.start-btn`과 `.next-btn`은 사실상 동일 스타일 → 통일 가능)
- `border-radius` 값이 4px~30px까지 19종 혼재
- `box-shadow` 78곳이 각각 다른 값으로 개별 정의됨
- 미디어쿼리 브레이크포인트 420px/480px/520px 혼용

### 1.5 접근성
- `prefers-reduced-motion` 대응 있음 → 반드시 유지

---

## 2. To-be (목표 디자인 방향)

### 2.1 톤앤매너
**따뜻하고 친근한 파스텔 캔디톤** — 민트, 코랄, 라벤더, 레몬 등 밝고 다양한 색을 사용해 학생 친화적인 분위기를 만든다. 사건배열게임의 고서/두루마리 컨셉은 이번 리뉴얼 범위에서 제외하며 기존 그대로 유지한다 (2026-08-18 결정).

### 2.2 컬러 시스템 — 학생색 기반 + 과목 보조색

**원칙: 학생색이 주역, 과목색은 보조.**
- 헤더, 주요 버튼, 진행바, 강조 요소 등 큰 시각 요소 → **학생 개인 색**
- 역사/수학 과목 구분 → 아이콘, 태그, 배지 등 **작은 강조 요소**에만 과목색 사용

#### 학생별 포인트 컬러 (확정 ✅ — 목업 검토 후 승인)
| 학생 | 컬러명 | Hex | 비고 |
|---|---|---|---|
| 김주하 | 코랄 | `#FF9B8F` | 밝고 따뜻한 코랄핑크 |
| 전민건 | 민트 | `#7FD8B5` | 청량한 민트그린 |
| 이하이 | 라벤더 | `#C6A8E8` | 부드러운 보라 |
| 최단비 | 레몬 | `#FFD97D` | 밝은 옐로우 |

각 학생색은 배경용 연한 톤(tint)과 텍스트/아이콘용 진한 톤(shade) 두 단계로 파생하여 사용한다. (예: 코랄 배경 tint `#FCE3DE`, 진행바/버튼 solid `#FF9B8F`, 텍스트용 shade `#D85A30`)

#### 과목별 보조 컬러 (확정 ✅ — 목업 검토 후 승인)
| 과목 | 컬러명 | Hex | 용도 |
|---|---|---|---|
| 역사 | 앰버 | `#F6DDB3`(배경) / `#633806`(아이콘·텍스트) | 아이콘/배지 |
| 수학 | 스카이 | `#CDEBF1`(배경) / `#04342C`(아이콘·텍스트) | 아이콘/배지 |

목업(학생별 학습 홈화면 4종, 2026-08-18 검토)을 통해 위 팔레트와 "학생색 기본 + 과목색 보조" 적용 비율을 확인했으며 수진님 승인 완료.

### 2.3 공통 뉴트럴 팔레트 (확정 ✅)

| 역할 | 기존 | 확정 |
|---|---|---|
| 페이지 배경 | #F4F7FC (차가운 블루톤) | `#FFFBF6` (웜 아이보리) — 목업에서 사용해 검증됨 |
| 카드/표면 | #FFFFFF | `#FFFFFF` 유지 |
| 진한 텍스트 | #1C2536 | `#2A2620` (부드러운 웜 차콜) |
| 보조 텍스트 | #7C8698 | `#8C8578` (웜톤 그레이) |
| 기본 보더 | 다양한 rgba 혼용 | `rgba(0,0,0,0.06)` 통일 |
| 정답 | #34C77B | `#4FAE6B` (파스텔에 맞게 채도 완화) |
| 오답 | #F0555C | `#E2575F` (김주하 코랄 `#FF9B8F`과 색상 충돌 없도록 좀 더 붉은 톤으로 조정) |

이 뉴트럴 팔레트는 학생색과 무관하게 전 화면 공통으로 적용된다.

### 2.4 타이포그래피
- 폰트 선택 기능 그대로 유지 (Gaegu/Noto Sans KR/Pretendard/Gowun Dodum/Nanum Myeongjo)
- 기본값(default) 폰트를 좀 더 친근한 인상의 서체로 조정할지 여부는 별도 논의 필요
- 사건배열게임의 `Song Myung`/`Noto Serif KR`은 리뉴얼 범위 제외로 그대로 유지

### 2.5 디자인 토큰화 (일관성 확보)
| 항목 | 현재 | 목표 |
|---|---|---|
| border-radius | 19종 혼재 | 3~4단계로 축소 (예: sm 8px / md 14px / lg 20px / pill 999px) |
| box-shadow | 78곳 개별 정의 | 2~3단계 elevation 토큰화 |
| 버튼 | 화면별 개별 클래스 파편화 | 공통 `.btn` 베이스 + `.btn-primary`/`.btn-secondary` 변형으로 통일, 학생색이 CSS 변수로 주입되는 구조 |
| 브레이크포인트 | 420/480/520px 혼용 | 1~2개로 통일 |

### 2.6 적용 범위
앱 전체 배경/버튼/포인트 요소 전반에 학생색 테마 적용. 학생이 선택/로그인된 시점부터 해당 학생색이 `--student-accent` 같은 CSS 변수로 주입되어 전 화면에 반영되는 구조를 제안.

### 2.7 앱 로고 (확정 ✅)

**"책 + 네 학생 컬러 점" 디자인으로 확정.**

- 배경: 중립 크림톤 뱃지 (`#FFFBF6`, 얇은 보더)
- 아이콘: 펼쳐진 책 형태, 플랫한 라인(기존 그라데이션 제거)
- 책등 위쪽에 학생별 포인트 컬러 4개 점(코랄/민트/라벤더/레몬)을 나란히 배치 — "네 학생 모두를 위한 앱"이라는 의미
- 기존 로고(파랑보라 그라데이션 배경 + 갈색 책 + 금박 두루마리)는 전체 리뉴얼 톤과 이질적이라 교체 대상에 포함
- 로고는 학생 개인색에 종속되지 않는 **중립 브랜드 마크**로, 모든 화면(시작화면 헤더, 학습홈 헤더 등)에서 동일하게 사용

**적용 위치**: `index.html`의 `.app-icon` 내부 인라인 SVG (시작화면 `#student-select-view`, 학습홈 `#learning-home-view` 두 곳에 중복 정의되어 있음 — 리뉴얼 시 동일 마크업으로 교체)

---

## 3. 파일별 예상 변경 범위

| 파일 | 변경 내용 | 비고 |
|---|---|---|
| `index.html` | `<style>` 블록 내 CSS 재작성 (색상 변수, radius/shadow 토큰, 버튼 클래스 통합) | HTML 구조(태그/id/class)는 최대한 유지, 스타일링용 class만 추가 |
| `app.js` | 학생 선택 시 `--student-accent` 등 CSS 변수를 주입하는 코드 **추가** (기존 로직 변경 없음) | 폰트 선택 로직(`applyFont` 등)과 유사한 패턴으로 최소 추가 |
| GAS 백엔드 (파일명 미상) | 변경 없음 | — |

---

## 4. 확인 필요 사항 (승인 요청)

1. ~~학생별/과목별 제안 컬러(2.2절) 확정 여부~~ → **확정 완료** (2026-08-18, 목업 검토 후 승인)
2. ~~로고 디자인 방향~~ → **확정 완료** (2.7절, "책 + 네 학생 컬러 점")
3. ~~공통 뉴트럴 팔레트~~ → **확정 완료** (2.3절)
4. ~~사건배열게임 고서 컨셉 순화 여부~~ → **범위 제외로 결정** (2026-08-18, 기존 그대로 유지)
5. ~~작업 순서~~ → **확정 완료**: 디자인 토큰(CSS 변수) 전체 정의 후 화면별 적용 (5절 참고)

**모든 확인 사항이 결정되어, 다음 단계(디자인 토큰 정의)로 진행 가능한 상태.**

---

## 5. 작업 순서 (확정 ✅)

**방식: 디자인 토큰(CSS 변수) 전체 정의 → 시작화면 1개 시범 적용 → 검토 → 나머지 화면 순차 적용**

1. CSS 변수 체계 재설계 — `:root`에 다음 토큰 정의
   - 학생색 4종 (`--student-coral`, `--student-mint`, `--student-lavender`, `--student-lemon` 및 각 tint/shade 파생값)
   - 과목색 2종 (`--subject-history`, `--subject-math` 배경/텍스트 쌍)
   - 뉴트럴 팔레트 (2.3절 값)
   - radius/shadow 토큰 (2.5절 3~4단계로 축소)
2. 시작화면(`#student-select-view`) 1개에 새 토큰 적용 → 실제 결과물로 검토·승인
3. 승인 후 나머지 17개 화면 순차 적용 (사건배열게임 화면은 제외)
4. `app.js`에 학생 선택 시 `--student-accent` 등 변수를 주입하는 코드 추가 (기존 로직 변경 없음)

각 단계는 별도 승인 후 진행하며, 한 화면 작업이 끝날 때마다 결과를 보여드리고 다음으로 넘어갑니다.
