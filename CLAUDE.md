# CLAUDE.md

이 파일은 이 저장소에서 작업하는 모든 세션에 자동으로 적용되는 규칙이다.
현재 진행 중인 작업은 **디자인(시각) 리뉴얼**이며, 상세 배경/근거는 `Design.md`(1차 — 학생별 파스텔 테마)와
`Design-2차-흥미유발.md`(2차 — 흥미 유발 비주얼/피드백 강화)를 참고한다. 2차는 1차 완료를 전제로 한 후속 작업이며,
아래 1차 Scope Lock은 계속 유효하고 2차 Scope Lock이 추가로 적용된다.

---

## 🔒 Scope Lock (범위 고정) — 반드시 준수

**이번 작업은 디자인(시각적 요소) 변경만 포함하며, 기존 기능은 100% 유지한다.**
아래 규칙은 사용자가 명시적으로 범위를 넓히라고 지시하기 전까지 모든 세션에서 그대로 적용한다.

### ✅ 변경 대상 (허용)
- 색상 팔레트 (컬러 시스템)
- 타이포그래피 톤 (폰트 선택 **기능**은 유지, 기본값/스타일만 조정)
- 컴포넌트 스타일 (버튼, 카드, 진행바, 배지 등 시각 스타일)
- 디자인 토큰 정리 (radius/shadow 값, 여백 값 통일)
- 레이아웃 (요소 배치, 여백 구조, 정보 순서 등 시각적 배치 개선) — 2026-08-18 추가
- 앱 로고 (`Design.md` 2.7절 — "책 + 네 학생 컬러 점" 디자인으로 확정)

### 🚫 절대 변경 금지 (Do Not Touch)
- **GAS 백엔드 로직** — 파일명 미상. 확인 전까지 이번 작업 범위에서 어떤 `.gs` 파일도 **열람·수정 금지**
- **`app.js`의 비즈니스 로직** — 데이터 흐름, 이벤트 핸들러, 상태 관리, `loadHistoryTrainingProgress()` 등
  - 단, `app.js`에 학생 선택 시 `--student-accent` 등 **CSS 변수를 주입하는 코드 추가**는 예외적으로 허용 (기존 로직은 변경하지 않음, `applyFont()`와 유사한 패턴으로 최소 추가만)
- **학생 ID 매핑, 진행률 계산 로직, `LEARNING_MODULES` 구조**
- **폰트 선택 기능** (`FONT_OPTIONS`, `applyFont()`) — UI 스타일 조정만 가능, 기능 자체(로직)는 유지
- **`MathProgress` 관련 Stage 2-A 설계 및 통신 로직**
- **"사건 배열하기" 게임의 고서/두루마리 컨셉** — `--ink`, `--parchment`, `--brass` 등 서브 테마 전체, 폰트(`Song Myung`, `Noto Serif KR`) 포함. 이번 리뉴얼 범위에서 완전히 제외 (2026-08-18 결정)
- **HTML 구조** — JS에서 참조하는 기존 `id`/`class` 이름. 스타일링을 위한 **class 추가는 가능**하나, 기존 id/class를 제거·변경하기 전에는 반드시 `app.js`에서의 참조 여부를 확인하고 개별 승인을 받는다

### 작업 원칙
1. **파일 수정 전 반드시 명시적 승인**을 받는다 (자동으로 진행하지 않는다).
2. **한 화면씩 단계적으로 진행**한다 — 여러 화면을 한 번에 일괄 변경하지 않는다.
3. **로컬 검증과 실제 배포 검증을 구분**하여 보고한다.
4. 위 "변경 금지" 항목에 해당하는 파일/로직을 열람하거나 수정해야 할 필요가 생기면, 먼저 사용자에게 **이유를 설명하고 승인**을 받은 뒤에만 진행한다.

---

## 🔒 2차 Scope Lock (흥미 유발 디자인) — 반드시 준수

**2차 작업은 "중·고생이 공부에 흥미를 갖도록" 하는 비주얼/피드백 강화이며, 기존 기능은 100% 유지한다.**
게이미피케이션(포인트/레벨/랭킹/출석보상 등 새 기능)은 범위 밖이다. 상세는 `Design-2차-흥미유발.md` 0절 참고.

### ✅ 변경 대상
- 정답 시 시각 피드백 (반짝임/통통 튀는 애니메이션)
- 진행바가 차오르는 애니메이션
- 성취 배지 (심플 아이콘 + 텍스트 스타일 — 이미 계산되는 완료 상태를 보여주는 표식이지, 새 보상 시스템이 아님)
- 기존 효과음 기능의 UI/UX 다듬기 (재생 로직은 이미 완성 — 아래 참고)
- 1차 파스텔 톤을 더 생기있게 강화 (팔레트 기본 hex는 유지, 강조 효과만 얹음)

### 🚫 절대 변경 금지
- 효과음 **재생 로직** (`SFX` 객체, `AudioContext` 합성, `toggleAppSound()`, `updateSoundToggleBtn_()`) — 이미 완성되어 있으므로 로직은 건드리지 않고, 필요 시 호출 지점/토글 버튼의 위치·스타일만 조정
- `SOUND_MUTE_KEY`(`appSoundMuted`) localStorage 키 및 음소거 저장 방식 — 그대로 사용
- GAS 백엔드 로직, 진행률 계산, 학생 ID 매핑, `LEARNING_MODULES`
- 폰트 선택 기능, 아바타 기능
- 사건배열게임 서브 테마
- 새로운 동기부여 기능(포인트/레벨/랭킹/출석보상 등) — 범위 밖
- HTML id/class 이름 (스타일링용 class 추가는 가능, 기존 참조 요소 변경 시 사전 확인)

### 작업 원칙 (1차와 동일)
- 애니메이션은 반드시 `prefers-reduced-motion: reduce`에서 비활성화/최소화한다.
- `app.js`는 class 부여/제거 수준의 최소 변경만 하며, 기존 함수 내용을 삭제·재작성하지 않는다. 실제로는 정답/완료/합격 시점에
  `.correct`/`.pass`/`.done` 등의 클래스가 이미 기존 로직에서 부여되고 있으므로, 2차 비주얼 작업 상당수는 `app.js`를 전혀
  건드리지 않고 `index.html`의 CSS 추가만으로 구현 가능하다 — 가능하면 이 방식을 우선한다.

---

## 프로젝트 구조 참고

- CSS는 별도 파일 없이 `index.html` 내부 `<style>` 태그 하나에 전부 작성되어 있다.
- 화면(screen) 18개가 SPA 방식(`display:none` 토글)으로 관리된다.
- 디자인 방향, 확정된 컬러 팔레트(학생색 4종/과목색 2종/뉴트럴), 토큰화 계획, 작업 순서 등 세부 사항은 `Design.md`를 참고한다.
- `Design.md`가 갱신되면 이 Scope Lock 섹션도 함께 최신 상태로 맞춘다.

## 반응형
- 모바일(375)
- 태블릿(768)
- 데스크탑(1180)